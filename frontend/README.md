# Frontend Workspace

The main project documentation lives in the repository root:

- [../README.md](../README.md)

This frontend is a React + TypeScript + Vite application that talks to the Go API through:

```env
VITE_API_BASE_URL=/api/v1
```
